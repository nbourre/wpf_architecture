<Window x:Class="SchoolManagement.UI.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchoolManagement.UI.Views"
        xmlns:vm="clr-namespace:SchoolManagement.UI.ViewModels"
        xmlns:views="clr-namespace:SchoolManagement.UI.Views"
        mc:Ignorable="d"
        Title="MainWindowView" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:StudentListViewModel}">
            <views:StudentListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StudentViewModel}">
            <views:StudentEditView />
        </DataTemplate>
                      
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Fichier"/>
            <MenuItem Header="Édition"/>
            <MenuItem Header="Affichage"/>
            <MenuItem Header="Enregistrement">
                <MenuItem>Nouveau</MenuItem>
            </MenuItem>
        </Menu>
        <ToolBar DockPanel.Dock="Top">
            <Button >Nouveau</Button>
        </ToolBar>
        <ContentPresenter Content="{Binding ViewModel}"/>
    </DockPanel>
</Window>
