<Window x:Class="SchoolManagement.UI.StudentsListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchoolManagement.UI"
        xmlns:attachedProperties="clr-namespace:SchoolManagement.UI.Helpers"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="DataGridCell">
            <Style.Setters>
                <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
            </Style.Setters>
        </Style>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="local:CustomCommands.New" CanExecute="AlwaysTrue_CanExecute" Executed="NewStudent_Executed"/>
        <CommandBinding Command="local:CustomCommands.Modify" CanExecute="AlwaysTrue_CanExecute" Executed="Modify_Click"/>
    </Window.CommandBindings>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Fichier"/>
            <MenuItem Header="Édition"/>
            <MenuItem Header="Affichage"/>
            <MenuItem Header="Enregistrement">
                <MenuItem Command="local:CustomCommands.New"/>
            </MenuItem>
        </Menu>
        <ToolBar DockPanel.Dock="Top">
            <Button Command="local:CustomCommands.New">Nouveau</Button>
        </ToolBar>
        <DataGrid x:Name="dg" ItemsSource="{Binding Students}" AutoGenerateColumns="False"
                  SelectedItem="{Binding SelectedStudent, Mode=TwoWay}"
                  attachedProperties:SelectingItemAttachedProperty.SelectingItem="{Binding SelectedStudent}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numéro" Binding="{Binding RegistrationNumber}"/>

                <DataGridTextColumn Header="Nom de famille" Binding="{Binding LastName}"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding FirstName}"/>
                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Margin="3 0" Padding="3" Command="local:CustomCommands.Modify">Modifier</Button>
                                <Button Margin="3 0" Padding="3" Content="Supprimer" Click="Delete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
